{
  "name": "myapp-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "vite",
    "build": "mkdir -p dist && echo '<h1>Master GitOps Pipeline — Frontend Running</h1>' > dist/index.html",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
